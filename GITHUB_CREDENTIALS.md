# GitHub Credentials

Relative path: `GITHUB_CREDENTIALS.md`

Purpose: This document records the preferred GitHub credential-management workflow for this repo and this machine. Use it when checking which GitHub account is active, correcting a wrong stored credential, or verifying safe push access to the repo. It exists to reduce account-switching mistakes and to keep local Git authentication aligned with repo ownership.

This repo uses an HTTPS remote:

`https://github.com/CodeByDerrick/agent-browser-tdd-relay.git`

The current recommended setup on this machine is:

- Use HTTPS remotes
- Use Git Credential Manager Core for authentication
- Use the `CodeByDerrick` GitHub account for this repo
