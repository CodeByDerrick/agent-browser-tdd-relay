# Doc Refresh

Relative path: `.agents/skills/doc-refresh/SKILL.md`

Purpose: This support skill refreshes only the docs affected by current work in `agent-browser-tdd-relay`. It is normally called from checkpoint or milestone ritual rather than directly. Use it when a ritual needs compact, targeted updates that keep docs accurate without bloating the maintenance pass.

This is usually called from `checkpoint-ritual` or `milestone-ritual`, not as a top-level entrypoint.
