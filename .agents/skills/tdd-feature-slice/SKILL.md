# TDD Feature Slice

Relative path: `.agents/skills/tdd-feature-slice/SKILL.md`
Project: Agent Bridge

Purpose: This support skill executes one minimal vertical slice from a brief inside Agent Bridge. It is normally called through `slice-ritual`, not as a top-level entrypoint. Use it when the ritual wrapper needs focused slice execution with bounded TDD behavior and concise evidence.
